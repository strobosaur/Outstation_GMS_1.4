///outside_view(x1, y1, x2, y2, length, block, length);

/*    var ret = 0;

    if (x > argument0) {x += x}
    if (y < argument4 > 
    if (x > argument4 > argument1 - argument5;
    if (y > argument0{
