///make_bonus_array(stats type, bonus type, name, op, amount, value, wpn type)

    var bonus = array_create(0);
    
    bonus[0] = argument0;
    bonus[1] = argument1;
    bonus[2] = argument2;
    bonus[3] = argument3;
    bonus[4] = argument4;
    bonus[5] = argument5;
    bonus[6] = argument6;

    return bonus;
