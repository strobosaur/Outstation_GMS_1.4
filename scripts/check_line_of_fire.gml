///check_line_of_fire(target, dir width, grid)

/*    var target = argument0;
    var width = argument1;
    var grid = argument2;
    
    var start = -(width div 2)
    var dist = point_distance
    
    for (var i = 0; i < width; i++)
    {
